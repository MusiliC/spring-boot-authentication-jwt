server:
  port: 6000
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userauthdb
    username: root
    password: makutano
  application:
    name: product-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update